#include <bits/stdc++.h>

using namespace std;
int c[10], n;

int main() {
    vector<int> a = {1, 2, 3};
    for (int b : a) cout << b << "\n";
    for (int i = 0; i < a.size(); i++) cout << a[i] << "\n";
    for (int d : c) cout << d << " ";
    return 0;
}