#include <bits/stdc++.h>  // C++ 의 표준 라이브러리가 포함된 헤더 파일

using namespace std;  // 변수명 중복 방지를 위한 범위 설정

// 상수 정의
#define PI 3.14159

string a;
typedef int
    i;  // 특정 클래스나 타입을 지정된 변수명으로 설정하여 사용 할 수 있다.

int main() {
    cin >> a;
    cout << a << "\n";

    i b;
    cin >> b;
    cout << b << "\n";

    cout << "상수 : " << PI << "\n";

    return 0;
}